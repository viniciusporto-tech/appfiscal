import {createClient} from "@/lib/supabase/server";import {formatDateTime} from "@/lib/utils/format";export default async function Page(){const s=await createClient();const {data}=await s.from("audit_logs").select("id,user_id,action,entity_type,entity_id,details,created_at,profiles(full_name)").order("created_at",{ascending:false}).limit(500);return <section><div className="topbar"><div><div className="eyebrow">Segurança</div><h1 className="page-title">Auditoria</h1><p className="page-subtitle">Histórico das principais alterações feitas no painel administrativo.</p></div></div><div className="card"><div className="table-wrap"><table><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Entidade</th><th>ID</th></tr></thead><tbody>{(data??[]).map((x:any)=><tr key={x.id}><td>{formatDateTime(x.created_at)}</td><td>{x.profiles?.full_name??"Sistema"}</td><td><strong>{x.action}</strong></td><td>{x.entity_type}</td><td>{x.entity_id??"—"}</td></tr>)}{(data?.length??0)===0&&<tr><td colSpan={5} className="empty-state">Nenhum evento de auditoria ainda.</td></tr>}</tbody></table></div></div></section>}
