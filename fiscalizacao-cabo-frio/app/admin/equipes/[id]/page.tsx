import { notFound } from "next/navigation"; import Link from "next/link"; import { TeamForm } from "@/components/admin/TeamForm"; import { createClient } from "@/lib/supabase/server";
type Props={params:Promise<{id:string}>}; export default async function Page({params}:Props){ const {id}=await params; const supabase=await createClient(); const {data}=await supabase.from("teams").select("id, code, name, description, active").eq("id",id).single(); if(!data) notFound(); return <section className="admin-form-page"><div className="topbar"><div><div className="eyebrow">Equipes</div><h1 className="page-title">Editar equipe</h1></div><Link className="button button-secondary" href="/admin/equipes">Voltar</Link></div><TeamForm initial={{id:data.id,code:data.code,name:data.name,description:data.description??"",active:data.active}} /></section>; }
